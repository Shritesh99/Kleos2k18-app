<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.technocracy.nitraipur.kleos2k18">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Navigation_Drawer" />
        <activity android:name=".activities.fragment1" />
        <activity android:name=".activities.fragment2" />
        <activity android:name=".activities.fragment3" />
        <activity android:name=".activities.fragment4" />
        <activity android:name=".activities.Fragment5"></activity>
    </application>

</manifest>